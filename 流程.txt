📌 ML/DL 專案 GitHub 更新完整流程筆記
1️⃣ 專案結構範例
Web_Learning_Model/
│
├─ app.py                 # Flask 主程式
├─ templates/
│   ├─ index.html
│   ├─ ml.html
│   └─ dl.html
├─ static/
│   ├─ css/
│   │   ├─ ML_html.css
│   │   └─ DL_html.css
│   └─ js/
│       ├─ ML_project_loader.js
│       ├─ DL_project_loader.js
│       ├─ ML_js/
│       │   ├─ project_1_add.js
│       │   └─ project_2_multiply.js
│       └─ DL_js/
│           ├─ project_1_cnn.js
│           └─ project_2_rnn.js
└─ ...

2️⃣ Flask HTML 靜態資源引用

Flask 下 HTML 必須用 url_for('static', ...) 方式引用 CSS / JS：

<!-- CSS -->
<link rel="stylesheet" href="{{ url_for('static', filename='css/ML_html.css') }}">

<!-- JS loader -->
<script type="module" src="{{ url_for('static', filename='js/ML_project_loader.js') }}"></script>


不建議使用 ../static/...，容易跨路由出現空白

ML / DL loader 建議分開兩個檔案：ML_project_loader.js、DL_project_loader.js

3️⃣ ML / DL loader 示例
ML_project_loader.js
import { project_1 } from "./ML_js/project_1_add.js";
import { project_2 } from "./ML_js/project_2_multiply.js";

export const ML_projects = {
  1: project_1,
  2: project_2
};

DL_project_loader.js
import { project_1 } from "./DL_js/project_1_cnn.js";
import { project_2 } from "./DL_js/project_2_rnn.js";

export const DL_projects = {
  1: project_1,
  2: project_2
};


每個專案單獨 JS 檔，方便維護和擴充。

4️⃣ Flask 路由設定
from flask import Flask, render_template

app = Flask(__name__)

@app.route("/")
def home():
    return render_template("index.html")

@app.route("/ml")
def ml():
    return render_template("ml.html")

@app.route("/dl")
def dl():
    return render_template("dl.html")

if __name__ == "__main__":
    app.run(debug=True)


/ml 對應 ML.html，/dl 對應 DL.html

靜態資源從 /static/... 讀取

5️⃣ 本地 Git 分支管理
統一 main 分支
git checkout main          # 切換到 main
git branch -u origin/main  # 設定追蹤遠端 main

刪除本地 master（可選）
git branch -d master       # 無未合併 commit
# 或
git branch -D master       # 強制刪除

刪除遠端 master（可選）
git push origin --delete master

6️⃣ 更新專案到 GitHub

每次修改 ML / DL 或 CSS / JS：

# 1️⃣ 檢查狀態
git status

# 2️⃣ 暫存修改
git add .

# 3️⃣ Commit
git commit -m "更新 ML/DL 專案與靜態資源"

# 4️⃣ 推送到 GitHub
git push


因為本地 main 已追蹤遠端 main，git push 就會自動更新遠端

不需要再指定分支

7️⃣ 瀏覽器測試

啟動 Flask：

python app.py


開啟瀏覽器：

http://127.0.0.1:5000/ → 首頁

http://127.0.0.1:5000/ml → ML 專案頁

http://127.0.0.1:5000/dl → DL 專案頁

確保 CSS / JS 正確載入。

8️⃣ 小技巧

每個 project 建議一個 JS 檔，ML_js / DL_js 分資料夾管理

loader JS 用 import/export 統一管理，HTML 只需載入 loader

使用 url_for('static', ...) 確保 Flask 下靜態資源不會壞掉

commit 訊息簡潔明瞭，例如：

更新 ML 專案程式碼

新增 DL 專案範例

這份筆記可以當作你 ML/DL 專案 GitHub 更新標準操作手冊，日後直接照做就能安全推送。